[add_note]
sql=CALL usp_insert_note(?, ?, ?, ?)
params=device_id, author, location, note
comment=Add a note to the database.  device_id should be the standard 36-character hex representation.

[get_all_notes]
sql=SELECT device_id, user_name, time, location_description, note_text FROM ViewNotes
